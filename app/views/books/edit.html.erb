<h1>Editing Book</h1>
<%= form_with model:p, url:"/books/#{@book.id}", local:true do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Opinion</h4>
  <%= f.text_area :body %>
  <%= f.submit 'Update Books' %>
<% end %>

<%= link_to 'Show', book_path %> |
<%= link_to 'Back', books_path %>

<footer>CopyRight Infratop.inc</footer>